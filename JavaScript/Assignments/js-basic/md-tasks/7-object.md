# **OBJECT**

## **Durasi**

- 5 hari
- Dikumpulkan hari **Jumat malam**

---

## **Cara Mengerjakan**

1. Clone repository

   ```bash
   git clone <link repository>
   ```

2. Buat branch masing-masing

   ```bash
   git checkout -b namabranch
   ```

3. Kerjakan dalam branch masing-masing

4. Add, Commit, Push

   ```bash
   git add .
   git commit -m "submit tugas object"
   git push origin namabranch
   ```

5. Pastikan push dilakukan ke branch masing-masing.

---

## **Tasks**

### **1. Object Sederhana**

Buat sebuah object `student` dengan properti:

- `name`
- `age`
- `class`

Tampilkan seluruh propertinya ke console.

---

### **2. Mengakses Properti Object**

Diberikan object berikut:

```js
const book = {
  title: "JavaScript Dasar",
  pages: 180,
  author: "Santino",
};
```

Tampilkan nilai `title` dan `author`.

---

### **3. Menambah Properti Baru**

Diberikan object berikut:

```js
const car = {
  brand: "Honda",
  year: 2021,
};
```

Tambahkan properti baru `color` dengan nilai `"red"`.

---

### **4. Object Dengan Method**

Buat object `calculator` dengan:

- properti: `x`, `y`
- method:
  - `add()` → mengembalikan hasil penjumlahan
  - `subtract()` → mengembalikan hasil pengurangan

---

### **5. Filter Array of Object**

Diberikan array berikut:

```js
const products = [
  { name: "Laptop", price: 7000 },
  { name: "Mouse", price: 150 },
  { name: "Keyboard", price: 350 },
  { name: "Monitor", price: 2000 },
];
```

Buat function yang mengembalikan daftar produk dengan harga **di atas 500**.

---

# **SELAMAT BEKERJA 🎉**
