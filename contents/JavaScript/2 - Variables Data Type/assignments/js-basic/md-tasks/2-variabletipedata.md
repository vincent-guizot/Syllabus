# VARIABLE dan TIPE DATA

## Durasi

- 5 hari, Di kumpulkan hari jumat malam

## Cara Mengerjakan

1. Clone repository

   ```
   git clone <link repository>
   ```

2. Buat branch masing - masing

   ```
   git checkout -b namabranch
   ```

3. Kerjakan dalam branch masing - masing

4. Add, Commit, Push

5. Push ke dalam branch masing - masing

   ```
   git push origin namabranch
   ```

## Tasks

### 1. Massa Jenis

- Tentukan Algoritma, Pseudocode dan Coding dari rumus berikut:

  rho = massa / volume

### 2. Selamat pagi

- Tentukan codingan dari hasil berikut:

  ```js
  "Selamat pagi, Admin";
  ```

  "Admin" bisa di ganti menggunakan variable

### 3. Angka Random

- Tentukan sebuah codingan untuk menghasilkan angka acak dari 1 - 10.

  ```js
  5; // Running pertama

  2; // Running kedua
  ```

### 4. Jumlah huruf dalam string

- Tentukan jumlah huruf dalam sebuah string

  ```js
  // selamat pagi => jumlah kata 12
  ```

### 5. Operator Logika

- Tentukan hasil dari soal berikut:

  ```js
  // 1
  let x = 1;
  let y = "1";

  let z = x == y;
  let p = y + x;
  let q = x === y;
  let r = y - x;
  let total = undefined == false || true;

  // 2

  // Tentukan hasil z, p, q , r dan total sebutkan alasan
  ```

> # SELAMAT BEKERJA
